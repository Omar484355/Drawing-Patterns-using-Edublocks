# Drawing-Patterns-using-Edublocks
Patterns are repeating  sequences of code.  Here we modify the triangle code  to draw a repeating, rotating  pattern.  The while True loop will run  forever, and the for loop will draw  the triangle.  Each time the loop iterates we  move the Turtle 10 pixels.
